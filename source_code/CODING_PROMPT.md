# 给 Codex 的生成提示词

你要生成一个本地教学平台，供软件课程学生学习 Android 自动探索测试、性质测试、错误报告和重放。

技术栈固定为 Python + FastAPI + Jinja2 + 少量 JavaScript + SQLite + openatx/uiautomator2。只支持本地 Windows 使用，不需要云服务和登录系统。

详细需求见 `README.md`。生成时优先保证这些主线：

1. 用户在本地网页填写设备号、APK 路径、测试用例数、每个用例最大事件数、算法、随机种子、性质概率、动作概率、动作间隔和稳定等待参数。
2. 平台使用 `adb -s <device_id>` 和 uiautomator2 连接设备，安装 APK，解析目标包名，每个测试用例开始前 force-stop、pm clear、清 logcat、启动应用。
3. 每个设备同一时间只允许一个任务，不同设备可以并行任务。
4. 支持随机、DFS、BFS 三种探索算法，并维护规范化 UI 状态图指导 BFS/DFS。
5. 允许学生在 `plugins/algorithms` 新增算法，算法只能接收当前状态、候选动作和状态图，返回动作，不直接控制设备。
6. 允许学生在 `plugins/properties` 新增性质函数。性质由前置检查、事件序列和最后一个 final assert 组成。只有最后 final assert 失败才算非崩溃错误。
7. 按用户设置概率在“普通探索”和“性质执行”之间选择。性质选择优先执行次数最少者，平局随机。
8. 每个 UI 动作后截图、保存 UI XML、记录事件。事件记录必须足够忠实以便不依赖随机种子重放。
9. 动作之间最少间隔 1 秒。动作后等待 UI 稳定，稳定规则为连续多次规范化状态相同。
10. 只测试目标包。离开目标应用后自动 Back；不能返回则重启目标应用。所有环境恢复动作也必须记录为 `SYSTEM_RECOVERY`，用于重放。
11. 崩溃只识别目标应用 logcat 中的 `FATAL EXCEPTION`。崩溃按异常类型和目标应用首个栈帧合并。
12. 非崩溃按性质 ID、最后断言类型和最后页面规范化表示合并。
13. 每个错误生成单独 HTML，总 HTML 展示本次运行概览。错误报告展示事件截图序列，高亮当前目标控件；Back、swipe、restart 显示动作标签；性质 final assert 失败时高亮整个页面外框。
14. 错误报告提供“出现误报”按钮，用户可选填匹配字段生成误报规则。未来命中规则的错误保留记录并标记 suppress，不结束探索。
15. 错误报告提供重放按钮。重放应重新安装/清理/启动目标应用，然后按原事件序列逐条执行，并独立记录重放结果。

UI 状态规范化规则：

- 保留 class、resource-id、content-desc、text、常见交互属性和父子结构。
- 忽略 bounds、index、package、focused、属性顺序。
- 仅纳入可见节点。
- 文本去除首尾空白，合并连续空白。
- 时间、纯数字、UUID、URL 替换为占位符。
- 同一个列表里重复出现的相同结构只算一次。
- 原始 XML、bounds、截图必须完整保存，供报告、高亮、重放和控件 count 断言使用。

实现顺序：

1. 先完成可运行的本地网页、SQLite 表、任务创建、任务详情页。
2. 再完成设备适配层和 APK 安装/启动/清理。
3. 再完成 UI 状态规范化、候选动作生成、三种内置探索算法。
4. 再完成性质插件协议和执行语义。
5. 再完成错误捕获、去重、误报规则、HTML 报告。
6. 最后完成重放和报告截图高亮。

