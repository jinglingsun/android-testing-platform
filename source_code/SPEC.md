# 需求规格初稿

## 目标

构建一个只在本地运行的 Android 自动测试教学平台。学生通过浏览器使用平台，通过 USB 真机或模拟器测试目标 APK。平台核心价值是让学生能观察探索算法、扩展探索算法、编写性质、查看错误报告、标记误报并重放错误。

## 平台边界

- 运行环境：Windows 本地。
- Web 形态：本地网页，默认监听 `127.0.0.1:8000`。
- 设备：支持模拟器和 USB 真机。
- 设备选择：用户通过 `adb devices` 获取设备号，然后输入平台。
- 并发：不同设备可分别创建独立任务；同一设备同一时间只能有一个任务。
- APK：用户提供本地 APK 路径，平台负责安装。
- 权限：第一版自动点击允许。
- 应用范围：只测试目标包。离开目标应用后自动返回；不能返回则重启目标应用。

## 任务配置

开始测试前提供配置页：

- 设备号：必填，无默认值。
- APK 路径：默认上次使用的 APK，首次为空。
- 测试用例数：默认 `3`。
- 每个用例最大事件数：默认 `50`。
- 探索算法：默认 `random`。
- 随机种子：默认自动生成。
- 普通探索/性质执行概率：默认性质概率 `0.2`。
- UI 动作类型概率：click、long_click、swipe、input、back，用户可自定义占比。
- 动作最小间隔：默认 `1s`，不可低于 `1s`。
- UI 稳定等待参数：轮询间隔、连续稳定次数、最大等待时间。

## 测试用例生命周期

每个测试用例开始时：

1. `am force-stop <package>`。
2. `pm clear <package>`。
3. 清理目标设备 logcat。
4. 启动目标应用。
5. 自动允许权限。
6. 等待 UI 稳定。

测试用例结束条件：

- 达到最大事件数。
- 找到未被误报规则抑制的崩溃错误。
- 找到未被误报规则抑制的性质违反。
- 环境恢复失败。

第一轮启动失败则任务失败。后续连续三次恢复失败则任务失败。

## UI 动作

普通探索初始支持：

- click：任意可见叶子控件，忽略控件自身 clickable 标记是否准确。
- long_click：任意可见叶子控件。
- input：优先 EditText、editable 或能触发键盘的控件。
- swipe：按坐标滑动。
- back：系统返回。
- restart：环境恢复动作。

每个动作记录：

- 动作类型。
- selector。
- bounds。
- 坐标。
- 输入文本或滑动方向等参数。
- 动作前状态哈希。
- 动作后状态哈希。
- 动作后截图。
- 动作后原始 UI XML。
- 时间戳。
- 等待和恢复动作。

动作之间最少间隔一秒。每个动作后等待 UI 稳定。稳定判定为连续多次规范化 UI 状态相同。

## 状态图与规范化

平台维护 UI 状态图，用于 DFS/BFS 等算法。

状态规范化规则：

- 保留 `class`、`resource-id`、`content-desc`、`text`、交互属性、父子结构。
- 忽略 `bounds`、`index`、`package`、`focused`、属性顺序。
- 只纳入可见节点。
- 文本规范化：去首尾空白、合并连续空白。
- 动态值替换：时间、纯数字、UUID、URL。
- 列表中重复出现的相同结构只算一次。
- 原始 XML 不丢弃，截图、bounds 和真实节点数量仍用于报告、重放和 count 断言。

## 探索算法

内置算法：

- random。
- DFS。
- BFS。

算法插件：

- 放在 `plugins/algorithms/*.py`。
- 插件类继承 `Explorer`。
- 输入当前状态、候选动作、状态图、随机数对象。
- 输出一个动作。
- 不直接访问设备，避免学生绕过平台记录。

## 性质

性质插件放在 `plugins/properties/*.py`，函数名以 `test_` 开头。

性质函数由三部分组成：

- 前置检查：第一个 UI 动作前的 `require_*`。
- 事件序列：tap、set_text、press_back 等平台包装动作。
- 后置检查：最后一个 `final_assert_*`。

判定语义：

- 前置检查失败：当前页面不适用，不算执行失败。
- 中间动作失败或中间 require 失败：性质执行不完整，不算错误，继续从当前页面探索。
- 只有最后的 `final_assert_*` 失败：性质违反，记录非崩溃错误。

性质调度：

- 每个稳定页面按用户概率选择普通探索或性质执行。
- 若选择性质，则从当前页面可执行的性质中选一个。
- 优先选择历史执行次数最少的性质，平局随机。
- 性质动作占用测试用例事件数。
- 性质开始和结束写入事件标记，重放时也保留。

## 错误

崩溃错误：

- 只识别目标应用 logcat 中的 `FATAL EXCEPTION`。
- adb、uiautomator2、平台自身异常归为环境错误。
- 去重依据：异常类型 + 目标应用首个栈帧。

非崩溃错误：

- 来自性质最后 `final_assert_*` 失败。
- 去重依据：性质 ID + 最后断言类型 + 最后页面规范化状态哈希。

误报：

- 错误报告提供“出现误报”按钮。
- 用户可选填匹配字段，例如应用版本、错误类型、性质、断言、页面、目标控件、异常、栈帧、动作序列。
- 命中误报规则的错误仍保留记录并标记 suppressed。
- suppressed 错误不结束探索。

## 报告

每个错误一个 HTML。另有任务总 HTML。

总报告包含：

- 执行时长。
- 进入过多少个页面。
- 性质数量。
- 每个性质执行次数。
- 找到多少错误。
- 错误列表和跳转链接。
- 状态图、算法队列/栈或访问顺序。

错误报告包含：

- 执行过的事件序列。
- 每个动作后的截图。
- 当前执行目标控件高亮。
- Back、swipe、restart 等无目标控件动作以标签或方向标注。
- 崩溃错误输出堆栈。
- 性质违反在最后截图上高亮整个页面外框，并输出违反的 assert。
- “出现误报”按钮。
- “重放复现”按钮。

## 重放

重放不依赖随机种子。测试时必须忠实记录每个动作。

重放流程：

1. 重新准备环境。
2. 清理并启动目标应用。
3. 按原事件序列逐条执行动作。
4. 每个动作仍等待至少一秒并等待 UI 稳定。
5. 对控件动作先尝试 selector 重定位，失败时使用记录坐标作为降级。
6. 环境恢复动作、权限动作、性质开始/结束标记都参与重放记录。
7. 若无法定位或最终错误不一致，则标记不可复现。

